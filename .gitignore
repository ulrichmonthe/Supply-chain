# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/

# Data / runtime
*.db
*.sqlite3
backend/var/
backend/uploads/
backend/exports/

# Node
node_modules/
.vite/
frontend/tsconfig.tsbuildinfo

# frontend/dist is deliberately COMMITTED. The product's whole claim is that it
# survives without its authors, and requiring a Node toolchain to see a map in a
# provincial health office defeats that. `make build` refreshes it.

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Env
.env
.env.local

# Playwright, installed ad hoc for UI verification
frontend/demo.cjs
frontend/tsconfig.tsbuildinfo
